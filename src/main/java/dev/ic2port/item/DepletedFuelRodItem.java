package dev.ic2port.item;

import net.minecraft.world.item.Item;

/**
 * Spent reactor fuel — inert inside the reactor grid but still hazardous in inventory.
 */
public class DepletedFuelRodItem extends RadioactiveItem {

    public DepletedFuelRodItem(final Properties properties) {
        super(properties);
    }
}
