package dev.ic2port.item;

import net.minecraft.world.item.Item;

/**
 * Empty tin can — used by the canner to package food (IC2-style).
 */
public class TinCanItem extends Item {

    public TinCanItem(final Properties properties) {
        super(properties);
    }
}
